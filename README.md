# mTools Library in Python
is a library where you can
make shortcuts from your
favorite libraries

Includes
* **pygame**
* **requests**
* **wmi**

Commands
   >>> import mtools
   >>> if is_connected:
           print("Connected to network")
   >>> if not is_connected:
           print("Unable to connect to network")

All rights reserved.
